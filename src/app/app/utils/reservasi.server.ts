import { doc, setDoc } from '@firebase/firestore'
import { Table_ReservasiLayanan } from './db.server'
import { ReservasiLayanan } from '~/models/reservasi'

export async function addReservasi(newReservasi: ReservasiLayanan): Promise<string | null> {
    // TO DO: NGULI YA ABANGKU
    return null
}